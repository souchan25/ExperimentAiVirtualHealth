[phases.setup]
nixPkgs = ["python310", "python310Packages.pip"]

[phases.install]
cmds = ["cd FastAPI && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'No build step required'"]

[start]
cmd = "cd FastAPI && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
